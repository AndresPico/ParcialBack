class AppConstants {
  AppConstants._();

  static const pageSize = 20;
  static const chatPollingInterval = Duration(seconds: 5);
  static const defaultLat = 4.710989;
  static const defaultLng = -74.07209;
}
